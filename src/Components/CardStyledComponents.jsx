import styled from 'styled-components'

export const SectionCard = styled.div`
  background-color: var(--color-primary);
  padding: var(--space-m);
  background-color: var(--color-primary);
  flex: 1;
  height: 30vh; 
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.210); /* Ombre subtile */
  transition: all 0.25s ease-in-out;
`
