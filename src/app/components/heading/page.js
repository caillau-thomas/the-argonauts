import React from 'react';
import styles from './heading.module.css';
import ScrollDown from './scrolldown/page';

const Heading = () => {
    return (
        <div>
            <ScrollDown targetId="main" />
        </div>
    );
};

export default Heading;
